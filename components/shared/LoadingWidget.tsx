import { Widget } from '@/registry/default/wigggle/widget';
import React from 'react';

const LoadingWidget = () => {
  return <Widget className="bg-secondary animate-pulse" />;
};

export default LoadingWidget;
